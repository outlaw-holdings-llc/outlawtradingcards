# Session handoff — outlawtradingcards.com — 2026-08-11 23:13

## Original session goal
Session began mid-stream (continued from prior). After closing out the IESS CA decommission and
adding yocaden.com house photos, the bulk was: **build outlawtradingcards.com from a static mock into
a working single-seller live-break/auction platform** (Noah's site — pull his Whatnot audience over).

## Trajectory
- Restored the lost BUILD-SPEC, committed it, then built **Phase 1** end-to-end: D1 backend + auth,
  admin console + customer account, KV image upload, Stripe payments (card-on-file + checkout +
  webhook), and a Drive→AI auto-catalog pipeline (vision classify + ensemble pricing).
- Stood up **live streaming** (Cloudflare Stream) — long debug session (HEVC vs H.264, Go-Live is a
  separate switch, viewer-side content blockers) ending in a **confirmed real viewer** (Noah).
- Fixed the pricing lowball ($99→ensemble), photo display, HEIC, added OG cards + flatpickr.
- Built **Phase 2 (the live room)** incrementally with the Gemini+Codex audit loop on each chunk:
  2a chat/viewers → 2b auctions → 2c breaks/razz → 2d moderation + web-push. Phase 2 is now complete.
- Ended by cleaning the system to a fresh state and writing the E2E validation/dress-rehearsal runbook.

## Final state
- **Live at outlawtradingcards.com** (Cloudflare Pages `outlawtradingcards`, main acct; DNS on Porkbun
  → pages.dev). Worker **`otc-live`** hosts the `LiveRoom` Durable Object (chat/auctions/breaks/mod).
- **Committed** (git repo /root/outlaw-holdings, all OTC work, ~30 commits this session; HEAD 3f5c3da).
  Every build committed as author "Claude" (classifier blocks impersonation + default-branch push).
- **Deployed:** Pages (site + Functions) + otc-live Worker. Secrets set: STRIPE_* (3), STREAM_API_TOKEN,
  INGEST_TOKEN, AUCTION_SECRET (Pages + Worker), VAPID_* (3), CF_ACCOUNT_ID var, D1 + KV + DO bindings.
- **Verified flows:** all builds E2E-tested over real WebSockets / API (presence, chat, auction
  lifecycle, break claim, razz commit, moderation, push plumbing) — BUT with NO-CARD test users so no
  real money moved. Live streaming verified with a real viewer.
- **System is CLEAN:** 0 shows, 0 orders, 0 push subs, 10 cards (8 seed + Dak Prescott auto + Cam Ward
  auto). Admins: christopher.outlaw@gmail.com, noahoutlaw24@gmail.com.

## What was tried and rejected (with reasons)
- **Durable Object inside Pages Functions** → Pages rejects (needs `script_name`, no `migrations`
  support). → separate `otc-live` Worker, Pages binds to it. THE key architecture decision.
- **R2 for card images** → not enabled (err 10042) → KV. **Cloudflare Stream** also needed enabling
  (Chris did it, paid). Both are dashboard/billing actions.
- **`api_token_full`** for D1/Pages/DO → err 10000 → must use **`api_token_account`** (cfat_).
- **Single-source pricing (Perplexity only)** → lowballed the Cam Ward auto $99 (priced non-auto) →
  3-engine ensemble + reconcile + "autograph" in the query.

## Open questions / blockers
- **Money paths never charged a real card** — auction-win + break-claim. Must validate before a real
  show (Part B/C of the runbook: save card, win $1 / claim $1, verify + refund).
- **Web-push delivery** unverified on a real device (Part E; iOS needs Add-to-Home-Screen first).
- Nothing blocked on a vendor. Next step is Chris + Noah's hands-on E2E test tomorrow, not more code.

## "Re-read me first thing next session"
1. **The DO lives in the `otc-live` Worker, NOT Pages** — changes touching real-time need TWO deploys:
   `wrangler deploy -c live-worker/wrangler.toml` then `wrangler pages deploy`. All CF ops use the
   **`api_token_account` (cfat_)** token.
2. **Phase 2 is feature-complete.** The remaining work is REAL-WORLD VALIDATION, not building:
   money paths with a real card, push on a device, a dress-rehearsal broadcast. Runbook DL:
   https://console.outlawholdingsllc.com/downloads/otc-validation-runbook.txt
3. **Tomorrow's plan:** Chris brings iPad, Noah sets up a stream + auction, run E2E + document. Claude
   = "control room" (watch orders/charges/stream/push from the server via `wrangler d1 execute` +
   Stripe API + the stream token).
4. Creds map: `/root/.otc-stripe-creds`, `/root/.otc-stream-creds` (stream token + VAPID +
   auction_secret), `/root/.otc-pipeline-creds`, `/root/.console-creds/cloudflare.json`.
5. `scripts/audit.sh <base-ref>` runs Gemini+Codex; use it on every future build chunk.

## File index — exact paths touched this session
- `outlawtradingcards.com/BUILD-SPEC.md` — the plan (Option-B, phasing)
- `outlawtradingcards.com/wrangler.toml` — Pages config (D1/KV/DO bindings, CF_ACCOUNT_ID var)
- `outlawtradingcards.com/live-worker/{wrangler.toml,src/index.js}` — otc-live Worker + LiveRoom DO
  (chat, presence, auctions, breaks, razz, moderation)
- `outlawtradingcards.com/functions/api/` — _lib.js, _stripe.js, _push.js, _stream.js; auth/*, cards/*,
  shipping/*, admin/{shows/*,orders,upload,review-queue}, live/socket.js, live.js, checkout.js,
  ingest.js, push/{config,subscribe}, internal/{auction-won,auction-reserve,auction-release,break-claim},
  stripe/{config,setup-intent,webhook}, payment-methods/*
- `outlawtradingcards.com/public/` — index/shop/auctions/live/about/account/console/checkout index.html,
  sw.js, assets/js/site.js, assets/css/site.css, assets/img/og-card.jpg, assets/vendor/{heic2any,flatpickr}
- `outlawtradingcards.com/db/` — schema.sql + migrate-001..004
- `outlawtradingcards.com/scripts/audit.sh`
- `/opt/otc-pipeline/ingest.py` — the Drive→AI pipeline (ensemble pricing)
- Runbooks in `/var/www/console-oh/downloads/`: otc-stream-setup.txt, otc-validation-runbook.txt,
  otc-stripe-key-rotate.txt
